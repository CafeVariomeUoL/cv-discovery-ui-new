setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile

run:
  web: envsubst "\$PORT" < /etc/nginx/nginx.conf.template > /etc/nginx/nginx.conf && service nginx start && uvicorn app.main:app --uds /var/sockets/uvicorn.sock


  # uvicorn app.main:app --reload --workers 4 --host 0.0.0.0 --port $PORT #uvicorn app.main:app --uds /var/sockets/uvicorn.sock